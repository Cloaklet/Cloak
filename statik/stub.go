package statik

/*
This file contains no code.
During release building `mage` will call `statik` tool to generate `statik.go` inside this directory
which will contain all the static assets for UI rendering.
*/
